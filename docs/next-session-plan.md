# 다음 세션 작업 계획 (2026-08-26 작성, 밤 갱신)

> 현재 상태: **v0.1 기능이 전부 들어갔다.** 배포·실데이터 76건·종단 검증·UI·폰트·헤더에 더해
> 마지막 둘(배경 색상 설정, IGDB 쿼터 + /admin 시스템 탭)까지 끝났다.
> 백엔드 테스트 **442개 초록**, 프론트 `tsc`·`lint`·`next build` 통과(에러 0, 경고 7은 기존),
> `contract-check` **25건 불일치 0**. Neon은 V2까지 적용돼 있고 **V3는 아직 안 올라갔다**.
> → **남은 건 V3 적용 + 배포 확인뿐이다.**
>
> 이 문서는 **설계 판단이 남지 않을 정도로** 촘촘하게 쓴 것이다. `[결정 필요]` 표시가 붙은 곳만
> 사용자 답이 있어야 하고, 나머지는 그대로 실행하면 된다.

## 배포 주소 (2026-08-26)

| 대상 | 주소 | 비고 |
|---|---|---|
| 프론트 (사용자가 접속) | https://starlog-xi.vercel.app | Vercel — 배포·환경변수는 대시보드에서 |
| 백엔드 (API) | https://starlog.onrender.com | Render 무료 — 15분 무활동 시 슬립, 콜드스타트 최대 50초+ |
| DB | Neon (`ep-restless-recipe-azqn40hd`) | 접속정보는 `backend/src/main/resources/application-local.yml` (git 미포함) |

`starlog.vercel.app`(접미사 없는 깔끔한 이름)은 선점되어 있어 `-xi`가 자동으로 붙었다.
Vercel 프로젝트 → Settings → Domains에서 다른 이름으로 재시도 가능 — 바꾸면 Render의
`CORS_ALLOWED_ORIGINS`·`FRONTEND_BASE_URL`도 같이 갱신해야 한다.

---

## 0. 세션 시작 시 30초 점검

```bash
cd ~/projects/Practice/starlog
git log --oneline -1                      # 헤더 개편 커밋이 최신이어야 함
cd backend && ./gradlew test              # 442개 초록 (도커 필요 — PostgresSchemaTest)
```

프론트는 `cd frontend && npx tsc --noEmit && npm run lint`.

⚠️ **도커가 꺼져 있으면 `PostgresSchemaTest`가 실패한다.** `open -a Docker` 후 20초.

---

## 작업 1. ~~Neon 스키마 재적용~~ — ✅ **2026-08-26 완료**

`drop schema public cascade` → `prod,local` 기동으로 새 V1 적용 → 스키마 17항목 검수 통과.
상세는 `docs/db-baseline-v1.md`의 "재적용 #2".

**Neon 현재 상태: 테이블 26개(V1 24 + V2 세션 2) + 실데이터 76건.** flyway 이력 V1·V2 두 줄.

⚠️ **V3(배경 팔레트 + usage_quota)는 아직 Neon에 안 올라갔다.** 배포하면 Flyway가 자동으로 올린다.
`alter table`과 `create table`뿐이라 기존 데이터는 안 건드린다

---

## 작업 2. ~~배포~~ — ✅ **2026-08-26 완료** (종단 검증까지)

**왜 최우선인가** — 반응형·PWA·일렉트론이 전부 이것의 하류다. 폰에서 열어봐야 반응형을 고칠 수 있고,
일렉트론은 로드할 URL이 있어야 한다.

### 2-1. 백엔드 → Render  [사용자 작업 + 내 작업]

**사용자가 할 것** (내가 못 하는 것: 계정·대시보드·환경변수 입력)
1. Render에서 `New > Web Service` → 이 저장소 연결 → **Root Directory `backend`**
2. **Docker 방식이다** — Render는 Java 네이티브 런타임이 없다(Node/Python/Ruby/Go/Rust만 있음).
   Root Directory를 backend로 잡으면 Render가 자동으로 Dockerfile을 찾아 Docker Build Context /
   Dockerfile Path를 `backend/`로 제안한다. `backend/Dockerfile`(멀티스테이지: JDK로 빌드,
   JRE로 실행)을 이미 만들어뒀고 로컬 `docker build` + 컨테이너 기동으로 검증 완료
3. Language 필드가 Node로 자동 감지되면 무시해도 된다 — Root Directory를 backend로 지정하는
   순간 Docker 옵션으로 바뀐다. Build/Start Command 칸은 Dockerfile이 대신하므로 비워둔다
4. Compute Plan은 **Free** — 계획 전제가 무료 티어다 (그래서 O-4로 세션을 DB로 옮겼다)
3. 환경변수 입력:

| 키 | 값 |
|---|---|
| `SPRING_PROFILES_ACTIVE` | `prod` |
| `DATABASE_URL` / `DATABASE_USERNAME` / `DATABASE_PASSWORD` | Neon 대시보드 값 (`application-local.yml`에 있음) |
| `IGDB_CLIENT_ID` / `IGDB_CLIENT_SECRET` | 〃 |
| `STORAGE_ENDPOINT` / `STORAGE_BUCKET` / `STORAGE_ACCESS_KEY` / `STORAGE_SECRET_KEY` / `STORAGE_PUBLIC_BASE_URL` | 〃 (R2) |
| `RESEND_API_KEY` | 〃 — ⚠️ **채팅에 평문 노출된 적 있음. 회전 권장** |
| `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` | 〃 |
| `ADMIN_EMAIL` / `ADMIN_PASSWORD` | 관리자 부트스트랩. `milo.beene@gmail.com`을 넣으면 **기존 회원을 승격**한다(멱등) |
| `CORS_ALLOWED_ORIGINS` | Vercel 주소 (2-2 후에 채운다) |
| `FRONTEND_BASE_URL` | 〃 |
| `SIGNUP_EMAIL_ALLOWLIST` | 기본값 그대로면 생략 |

**내가 할 것**: `application-prod.yml`에 아직 없는 키가 있으면 채우고, 기동 로그로 검증.

### 2-2. 프론트 → Vercel  [사용자 작업]
1. `New Project` → **Root Directory `frontend`**
2. 환경변수 `NEXT_PUBLIC_API_BASE` = Render 백엔드 URL
3. 배포 후 나온 Vercel URL을 Render의 `CORS_ALLOWED_ORIGINS`·`FRONTEND_BASE_URL`에 넣고 재배포

### 2-3. 구글 OAuth 리디렉션 URI 추가  [사용자 작업]
콘솔 → `Credentials` → OAuth 클라이언트 → **승인된 리디렉션 URI**에
`https://{render주소}/login/oauth2/code/google` 추가. 안 하면 구글 로그인이 `redirect_uri_mismatch`로 죽는다.

### 2-4. ~~O-4 Spring Session JDBC~~ — ✅ **2026-08-26 완료**

**이걸 배포 뒤로 미루면 안 되는 이유**: Render 무료는 **15분 무활동이면 JVM이 죽는다.**
세션이 메모리에 있으므로 **깰 때마다 전원 로그아웃**된다. 지인 몇 명이 가끔 쓰는 패턴이면
사실상 매번 로그인해야 한다 — 앱이 못 쓸 물건이 된다.

- `spring-session-jdbc` 추가, 세션 테이블은 **마이그레이션 V2로 직접 작성**
  (Spring Session이 제공하는 스키마를 그대로 쓰되, V1과 같은 손감사 규칙 적용)
- ⚠️ **`SessionRegistry`도 같이 봐야 한다.** 지금은 JVM 메모리라 재시작 시 비고,
  그러면 전 세션 무효화(FR-AUTH-05·09)가 조용히 0건이 된다 — 오늘 고친 그 버그와 같은 종류다.
  `SpringSessionBackedSessionRegistry`로 교체할 것
- 검증: `PostgresSchemaTest`에 세션 테이블 제약 단언 추가 + 재시작 후 로그인 유지 수동 확인

### 2-5. ~~O-5 HikariCP 튜닝~~ — ✅ **2026-08-26 완료** (`maximum-pool-size: 5`, prod 전용)
Neon 무료는 유휴 시 컴퓨트를 재운다. 기본 풀(10)은 무료 티어에 과하다.
`maximum-pool-size: 5`, `idle-timeout`·`max-lifetime`을 Neon 유휴 임계보다 짧게.
`application-prod.yml`에만 넣는다 (dev H2는 그대로).

### 2-6. ~~배포 후 종단 검증~~ — ✅ **2026-08-26 완료**

| 항목 | 결과 |
|---|---|
| 이메일 로그인 / 구글 로그인·연동 | ✅ (DB 대조까지) |
| 담기 → 회차 → 취득 → 통계 | ✅ 실데이터 76건 임포트로 전 경로 통과 |
| 커버 업로드 | ✅ R2 CORS 설정 후 확인 |

**커버 업로드가 막혔던 이유** — 브라우저가 R2로 **직접 PUT**하는데 그건 단순 요청이 아니라
(PUT + Content-Type) 프리플라이트가 먼저 나간다. R2 버킷에 CORS 정책이 없어 403이었다.
Cloudflare R2 대시보드에서 아래를 넣어 해결했다:

```json
[{ "AllowedOrigins": ["https://starlog-xi.vercel.app", "http://localhost:3000"],
   "AllowedMethods": ["PUT"], "AllowedHeaders": ["content-type"], "MaxAgeSeconds": 3600 }]
```

`GET`이 없는 이유 — 이미지는 `<img>`로 불러오는데 그건 CORS 대상이 아니다.
`content-type`이 필요한 이유 — 그 값이 presigned 서명에 포함돼 있어, 다른 값으로 올리면 R2가 403을 준다.

> **이미지가 어디 저장되나** — 개인 커버만 R2다. 마스터 커버·배너는 IGDB CDN(`images.igdb.com`)에서
> 오고 우리 서버를 안 거친다. DB에는 `coverImageId`만 있고 화면이 크기별 URL을 조립한다

### 2-7. 배포 후 성능 — **콜드스타트는 수용하기로 확정 (2026-08-26)**

실측: 로컬 3ms / 배포 깨어있음 **150ms** / 잠들었다 깨면 **최대 50초+**.

150ms의 내역 — 한국→Cloudflare 엣지(오사카) ~50ms, 엣지→Render 싱가포르 ~80ms, 앱 ~20ms.
**Render 무료는 리전에 도쿄·서울이 없어** 싱가포르가 이미 최선이다. 이 왕복은 구조적이라 못 줄인다.

**핑으로 깨워두는 우회는 하지 않는다.** Render 공식 문서에 외부 핑에 대한 언급은 없고
(제재 조항은 서비스가 **내보내는** 트래픽에 대한 것뿐이라 명시적 위반은 아니다),
그럼에도 Render가 제시하는 해법은 유료 인스턴스뿐이다 — 15분 슬립이 무료 티어의 가격 근거라
명시 금지가 없어도 의도에 반한다. 실사용이 늘어 정말 불편해지면 **$7 Starter가 정직한 답**이다.
(덤: 잠든 동안 `/robots.txt` 요청은 Render가 가로채 직접 응답해서 깨우지도 못한다)

---

## 작업 3. ~~옵시디언 재투입~~ — **폐기 (2026-08-26 결정)**

임포터는 만들지 않는다. 실데이터는 배포 후 화면에서 직접 담는다.

참고로 `VaultLoader`는 **git에 커밋된 적이 없어** 복구도 불가능했다(전체 이력 744파일 확인) —
인수인계 문서의 "이력에서 부활시키면 된다"는 서술은 틀린 것이었다.

### DB 초기화는 이미 끝났다
"엔티티가 많이 바뀌었으니 테이블 다 내리고 다시 검수해서 쿼리 날려야 한다"는 판단은 맞고,
**그 작업을 2026-08-26 밤에 이미 했다**:
- V1~V3 마이그레이션을 청산하고 현재 엔티티 기준으로 **V1을 손으로 다시 작성** (`db-baseline-v1.md`)
- 3렌즈 미니 리뷰로 검수 → 정렬 인덱스 2종·방향 명시·auth_token 인덱스 보강
- Neon `drop schema` → 새 V1 적용 → API 왕복 검증 → 검증 데이터 청소

**그 뒤 결정이 한 번 더 뒤집혔다** — 배포가 끝난 뒤 사용자가 볼트 md를 직접 넣어달라고 해서
결국 일회성 스크립트로 76건을 투입했다. 재사용 가능한 임포터를 안 만든다는 결론은 그대로다.
자세한 건 아래 "실데이터 투입" 절.

---

## 작업 4. ~~입구 연출·폰트·UI·사이드바~~ — ✅ **2026-08-26 완료** / 반응형은 보류

### 완료

| | 내용 |
|---|---|
| **입구 로딩 연출** | 가로로 훑는 빛 + 라벨. 최소 1.5초 노출, 재방문 시 없음(useSession 모듈 캐시) |
| **배경 팔레트** | 한강 노을 사진 기반. 값은 작업 4-2 표 참고 |
| **폰트** | 영문 Switzer / 한글 Pretendard / 숫자 JetBrains Mono — 전부 로컬 가변 폰트 |
| **폰트 무게** | 43KB + 2.0MB + 296KB = 2.3MB. `font-display: swap`이라 글자를 막지는 않지만 Pretendard 서브셋과 JetBrains woff2 변환은 남은 숙제 |
| **파비콘** | `app/icon.svg`. `prefers-color-scheme`으로 밝은 탭/어두운 탭 대응 |
| **UI 4건** | 계정 소속 플랫폼 표시 · 취득 플랫폼→계정 순서 · 구글 연결 해제(비밀번호 있으면 허용) · 사이드바 태그 그룹 |
| **금액 표기** | `Money` 컴포넌트 — `Intl.formatToParts`로 통화기호는 sans, 숫자만 모노. `₩`이 모노라 어긋나 보이던 문제 |
| **오버라이드 표시** | 마스터를 덮어쓴 항목은 `text-teal-200/70`. 장르에만 빠져 있던 것도 채웠다 |
| **헤더 개편** | 좌 워드마크 / 중앙 심볼(장식) / Dashboard·Library·Add / 우 프로필. 색 반전 |

### 배운 것 — `mix-blend-mode`는 **요소 자신에** 걸어야 한다

헤더 글자를 배경에 따라 반전시키는 데 세 번 헛짚었다. 전부 같은 원인이었다.

- **자식에 걸면 안 된다.** z-index가 붙은 조상은 쌓임 맥락을 만들고, 자식의 블렌딩은
  그 맥락 **안에서만** 일어난다 — 헤더의 투명 배경하고만 섞여 아무 일도 안 생긴다
- **요소 자신에 걸면** 그 요소의 backdrop(부모 맥락에서 먼저 그려진 전부)과 섞이므로
  z-index를 줘도 된다. z-index를 뺐다가 본문이 헤더 위로 올라와 클릭을 가로챈 것도 이것 때문이었다
- **자식은 조상의 블렌딩에서 빠져나올 수 없다.** 드롭다운 패널이 형광색으로 뒤집히는 걸 막으려면
  `createPortal`로 body에 빼야 한다 (`Dropdown`의 `portal` 옵션, 기본 false)
- **흑백으로 떨어뜨리려던 시도는 폐기.** `backdrop-grayscale`로 블렌딩 전에 배경 채도를 빼면
  검정↔흰색이 되지만, 헤더 띠만큼 배경이 흑백이 되어 더 나빴다. 보색이 나오는 쪽을 택했다

### 리액트 19 린트가 잡아준 것 둘

`npm run lint`가 **경고가 아니라 에러**로 막았다. 둘 다 빌드는 통과하던 코드다.

- **`react-hooks/refs` / `purity`** — 입구 페이지에서 마운트 시각을 렌더 중에 찍고 있었다
  (`if (ref.current === 0) ref.current = Date.now()`). ref 쓰기도 `Date.now()`도 렌더를
  불순하게 만든다 → effect 첫 실행에서 찍는다. 마운트와의 차이는 몇 ms라 연출에 영향 없음
- **`react-hooks/set-state-in-effect`** — 설정 페이지의 "해제하는 중"을 effect에서 껐다.
  **상태가 아니라 파생값**이 맞다: `unlinkRequested && (me.data?.profile.googleLinked ?? true)`.
  effect로 끄면 렌더가 한 번 더 돈다

### 반응형 — **보류 (웹 유지 결정 뒤에)**

로컬 앱(v1.0)으로 전환하면 버려지는 작업이라 결정이 선 뒤에 한다. 조사 결과는 남겨둔다:

| 곳 | 현재 | 할 일 |
|---|---|---|
| `LibrarySidebar` | `w-64` 고정 + `mt-20 ml-6` | 왼쪽만 화면에 붙이고 나머지는 띄운 채. 기본은 얇게, 토글로 확장 |
| `AppHeader` | 좌·중앙·우 절대 배치 | 좁으면 심볼 + 햄버거 + 프로필로 축소 |
| `FilterBox` | `grid-cols-1 sm:2 lg:3 xl:5` | 접히긴 하지만 모바일에서 세로가 길다 — 접이식으로 |
| 다이얼로그 7개 | `grid-cols-2` 고정 | `grid-cols-1 sm:grid-cols-2` |
| `FluidBackground` | 항상 WebGL | 모바일·`prefers-reduced-motion`이면 정적 그라데이션 (배터리·발열) |

**이미 되어 있어 손댈 것 없는 곳**: `DataTable`(`overflow-x-auto`), 상세 페이지(`lg:grid-cols-3`),
라이브러리 그리드(`2 → 3 → 4 → 6 → 8` 램프).

---

## 작업 4-2. ~~배경 색상 설정~~ — ✅ **2026-08-26 완료**

색 5개만 노출한다. 숫자 5개(속도·주기·채도·명도)는 서로 얽혀 있어 하나를 만지면 다른 인상이
같이 바뀐다 — 실제로 순회 속도를 올렸더니 "흐름이 달라졌다"는 지적을 받았고, 명도를 흐름 노이즈에
태웠더니 색 순회가 안 보이게 됐다. **사용자가 만지면 대부분 나빠지는 값이라 안 연다.**

| | |
|---|---|
| 위치 | 설정 → 프로필 수정 → 닉네임 아래 |
| 저장 | `member.background_colors varchar(64)` — `#rrggbb` 5개를 쉼표로. **null = 기본값** |
| 프리셋 | `lib/palette.ts`의 `PALETTES`. 지금은 한강 노을 하나 |
| 미리보기 | 16:9 한 장, `split=0.5` — 왼쪽 입구 / 오른쪽 앱 내부 |

**null을 유지한 이유** — 기본 팔레트를 나중에 바꾸면 한 번도 안 만진 회원이 자동으로 따라온다.
가입 시점의 색이 박제되지 않는다.

### 이 작업이 드러낸 것 셋

- **`useSession`이 스토어가 아니었다.** 부르는 컴포넌트마다 `/api/me`를 따로 때리고 각자
  스냅숏을 들었다 — 화면 하나에 같은 요청이 네 번, 그리고 **저장해도 배경이 안 바뀌었다.**
  `useSyncExternalStore` 기반 모듈 스토어로 바꿨다. 훅 시그니처는 그대로라 호출부는 안 건드렸다
- **미리보기는 진짜 셰이더여야 한다.** CSS 그라디언트로 흉내 내면 저장 뒤 다른 그림이 나온다.
  셰이더를 `lib/fluidShader.ts`로 빼고 `FluidCanvas` 하나를 전역 배경과 미리보기가 공유한다
- **`uSplit`은 색만 가른다. 흐름은 안 가른다** — 속도까지 갈랐으면 두 반쪽이 서로 다른 시각을
  흘러 가운데에 이음매가 생긴다

---

## 작업 5. PWA  (반응형 직후, 거의 공짜)
`manifest.json` + 아이콘 + `next-pwa` 또는 수동 서비스워커. 홈 화면 아이콘 + 전체화면.
**오프라인 캐싱은 하지 않는다** — 데이터가 서버에 있고 캐시 무효화 비용만 는다.

---

## 작업 6. ~~IGDB 안내 + 일일 쿼터 + /admin 시스템 탭~~ — ✅ **2026-08-26 완료**

근거와 수치는 `docs/capacity-planning.md`. **셋 다 WEB-ONLY다** — 로컬 앱 전환 때
떼어낼 것으로 표시해뒀다 (`docs/web-only-inventory.md`).

### 6-1. 전역 게이트 — 세마포어 + 즉시 반려
`Thread.sleep` 무한 대기를 `Semaphore(6, fair)` + `tryAcquire(800ms)`로 바꿨다.
자리를 못 잡으면 **큐에 세우지 않고 429**로 돌려보낸다:
> "지금 여러 분이 동시에 검색 중입니다. 바로 다시 시도해 주세요"

세마포어(동시성)와 최소 간격(처리율)은 **다른 것을 막는다.** 세마포어만 두면 6개가 동시에
나갔다 돌아와 다시 6개가 나가 초당 4건을 넘고, 간격만 두면 느린 응답이 쌓여 동시 열린 요청이 8을 넘는다.

### 6-2. 일일 쿼터 — DB 카운터
`usage_quota(member_id, usage_date, kind, used)`, **복합 PK**. 검색 200 / 담기 50 / 커버 20.

- **인메모리가 아닌 이유**: Render 무료는 15분 무활동에 프로세스를 내린다. 매번 0이면 장식이다
- **네이티브 upsert를 안 쓴다**: `on conflict`(PG) / `merge into`(H2) 문법이 갈려 dev와 prod가
  다른 코드를 탄다 → "UPDATE 해보고 0행이면 INSERT, 제약 위반이면 UPDATE로 후퇴"
- **세기 전에 본다**: 올리고 확인하면 벌크 UPDATE가 영속성 컨텍스트를 우회한 직후를 읽어
  옛 값을 볼 수 있다 (설계 원칙 13). 한도 검사는 스칼라 조회로 한다
- **컨트롤러에 문다**: 쿼터는 도메인 규칙이 아니라 웹 엣지의 정책이다. 서비스에 넣으면
  로컬 앱으로 갈 때 도메인을 헤집어야 한다

### 6-3. 설정 화면의 남은 쿼터
`GET /api/me/quota` → "검색 12/200" 막대. 80%에서 노랗게, 100%에서 빨갛게.
**빈 배열이면 섹션을 통째로 안 그린다** — 로컬 앱 빌드에서 백엔드 프로파일 하나로 화면까지 사라진다.

### 6-4. `/admin` 시스템 탭
IGDB 누적 호출·반려 수 / 커버 수·용량 / DB 크기 / 오늘 회원별 쿼터.
**Actuator는 안 붙였다** — 단일 인스턴스가 15분마다 재시작해 요청 수 통계가 계속 0으로 돌아가
숫자가 의미를 못 가진다. 필요한 값은 전부 DB와 인메모리 카운터로 나온다.

### 여기서 난 진짜 버그 하나

`pg_database_size()`는 H2에서 **반드시 실패한다.** 예외를 잡았는데도 화면이 401로 죽었다 —
실패한 쿼리가 **트랜잭션을 rollback-only로 표시**해서 커밋 때 `UnexpectedRollbackException`이
터진 것이다. 잡는 것만으로는 부족했다.
→ 클래스 레벨 `@Transactional`을 떼고, 그 쿼리만 `JdbcTemplate`(제 커넥션)으로 돌린다.
`SystemStatusTest`가 이 경로를 지킨다.

---

## 작업 7. 일렉트론  (맨 마지막)

**선행 조건**: 배포 완료 (로드할 URL 필요).
**핵심 난관**: 구글이 임베디드 웹뷰 OAuth를 차단한다 → 시스템 브라우저 + 딥링크.
도메인을 안 사기로 확정했으므로 **이 우회는 선택이 아니라 필수**다.

- 백엔드에 일회용 토큰 교환 엔드포인트 신설
- 앱은 `starlog://` 커스텀 스킴 등록, 시스템 브라우저에서 로그인 → 토큰으로 세션 교환
- 앱 본체는 **배포 URL을 로드하는 얇은 껍데기** (프론트 번들 X — CORS·쿠키 문제가 되살아난다)

---

## 알려진 한계 3건 — **문서로 둔다 (2026-08-26 확정)**

재발송 스로틀 레이스 / 플랫폼 삭제 경합 / 병합 레이스.
1인 습작 + 지인 몇 명이면 도달 확률이 극히 낮다. 근거는 `docs/code-review-2026-08-26.md` 마지막 표.
**고치지 않는다** — 사용자가 늘거나 실제로 겪으면 그때 재검토.

---

## 우선순위 요약 (2026-08-26 갱신)

```
✅ v0.1 완료 — 전부
   태그 단일화 · Neon V1/V2 · 세션 DB화 · 풀 튜닝 · 배포 · 실데이터 76건
   커버 업로드 종단 확인 · 입구 로딩 연출 · 배경 팔레트 · 폰트 3벌 · 파비콘
   UI 4건 · 사이드바 태그 그룹 · 헤더 개편(색 반전) · 대시보드 한 줄+페이드
   심볼 장난감 · 배경 색상 설정 · IGDB 게이트 + 일일 쿼터 + 시스템 탭
   월별 지출 툴팁에 항목 이름 · 그리드 제목 한 줄 · 검색창 지우기 버튼
   Field label 위임 버그 · 쿼터 타임존(KST) · 정리 배치 기동 따라잡기 · 필터 플랫폼명
   사이드바 전역 무효화 · 관리자 무제한 · 삭제 되살리기 화면 · IME 가드 · 날짜 선택기
   옵시디언 메모 43건

── v0.1 출시 절차 ─────────────────────────
0-1. Neon에 **V3 적용** (앱을 prod로 한 번 띄우면 Flyway가 올린다)
0-2. 커밋 → 푸시 → Vercel/Render 재배포
0-3. 프로덕션에서 눈으로 확인 (아래 목록)

── v0.1 이후 ──────────────────────────────
1. 반응형 + PWA      ← **웹 유지가 정해진 뒤에만**

── v1.0 ──────────────────────────────────
2. 로컬 앱 — 계획은 docs/web-only-inventory.md
```

**배포 후 눈으로 볼 것** (전부 로컬에서만 확인했다)

| | 왜 프로덕션에서 봐야 하나 |
|---|---|
| 배경 색상 저장 | 브라우저 `input[type=color]`의 생김새가 OS마다 다르다 |
| 미리보기 캔버스 | **WebGL 컨텍스트가 두 개**가 된다. 저사양 기기에서 하나를 잃을 수 있다 |
| 헤더 blend / 포탈 드롭다운 | 합성 경로가 브라우저마다 갈린다 |
| 대시보드 `mask-image` 페이드 | 〃 |
| 폰트 2.3MB | Pretendard가 2MB — 느리면 서브셋 |
| 시스템 탭의 DB 크기 | **PostgreSQL에서만 값이 나온다.** 로컬(H2)에서는 항상 `—`라 여기서 처음 확인된다 |

**왜 반응형이 아직 미정인가** — 로컬 앱으로 전환하면 버려지는 작업이다(폰 지원이 전제).
그 결정은 실제 사용 빈도를 며칠 봐야 나온다 — 자주 들어가면 콜드스타트를 거의 안 만나고,
3일에 한 번이면 매번 만난다.

**폐기**: 옵시디언 임포터(일회성으로 끝), 알려진 한계 3건 수정, 콜드스타트 우회 핑.

---

## 실데이터 투입 — ✅ **2026-08-26 완료 (76건)**

옵시디언 볼트(`~/Core Vault/03. Collection/Games`) md 78개 → 인덱스 1개 제외 77건 →
마리오 3D 월드 + 퓨리 월드 병합으로 **76건** 투입.

- **마스터는 IGDB.** 볼트 제목이 한글이라 IGDB(영문 DB) 검색이 안 돼서, 영문 제목을 대고
  찾은 뒤 **볼트 출시일과 대조**해 76/76 전건 검증했다. 매칭표는 `docs/igdb-match.md`
- **표시명은 볼트 한글명**을 오버라이드로 넣어 `displayName`이 거기서 파생되게 했다.
  IGDB의 한글 별칭은 대부분 약칭(`젤다 야숨`, `모동숲`)이라 표시명으로 못 쓴다
- 넣은 것: 제목·평점·취득(가격/플랫폼/계정/날짜)·회차(기간/기기/에뮬/입력수단/계정)·개인 장르
- 안 넣은 것: 커버·개발사·출시일 — IGDB 마스터가 대신 갖는다
- 검수: 항목 76 / 취득 76 / 회차 64 / 지출 합계 1,885,530원 — 볼트 원본과 일치

**임포트가 드러낸 것 넷**
- `paused: true`를 회차 전부에 적용하면 2024년에 끝낸 회차까지 중단이 된다 → 최근 회차에만
- `바이패드`는 IGDB에 없었다. 커버 파일명 `cover-biped.jpg`가 단서였다 (Biped)
- 매칭을 날짜 근접도만으로 고르면 DLC·버전판을 집는다 → **이름 정확 일치를 우선**
- 볼트 원본 결함: 몬헌 2건의 장르에 **문자 그대로** `\t\r`이 박혀 있었다(제어문자가 아니라
  백슬래시 두 글자라 `strip()`으로 안 지워진다)

**취득일·플레이시간이 비어 있는 이유가 밝혀졌다** — 임포터 버그가 아니라 **원본 md에 그 필드가 없다.**

---

## 결정 완료 — 게임당 태그 **하나** (2026-08-26)

`backlog_entry_tag` 조인 테이블 폐기 → `backlog_entry.tag_id` nullable FK.
근거·감수·구현 범위는 `docs/spec-v1.5.md` §6.7의 "항목당 태그는 하나다" 박스와
`docs/db-baseline-v1.md`에 적어뒀다.

**딸려온 것**: 폴더 뷰가 태그마다 목록 API를 때리던 1+N 요청이 한 방이 됐다.
